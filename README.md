# property_listings

Static property listing website with:

- Adibatla flat listing
- Vattinagulapally open plot listing
- Local image assets under `assets/`

Open `index.html` locally or publish via GitHub Pages / any static host.
